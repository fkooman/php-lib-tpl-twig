language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0

before_install:
  - sudo apt-get update && sudo apt-get --reinstall install -qq language-pack-nl language-pack-fr

before_script:
  - composer install

script: phpunit --coverage-text tests
